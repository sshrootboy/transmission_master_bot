# 🚀 Быстрый старт

Запустите бота за 5 минут!

## Шаг 1: Создание структуры (30 сек)

```bash
mkdir telegram-transmission-bot
cd telegram-transmission-bot
mkdir -p bot transmission downloads watch
```

## Шаг 2: Получение токена бота (2 мин)

### Создание бота
1. Откройте Telegram
2. Найдите **@BotFather**
3. Отправьте: `/newbot`
4. Введите имя: `My Transmission Bot`
5. Введите username: `my_transmission_bot`
6. **Скопируйте токен** (выглядит как `1234567890:ABCdefGHIjklMNOpqrs`)

### Получение вашего User ID
1. Найдите **@userinfobot**
2. Отправьте: `/start`
3. **Скопируйте ваш ID** (число, например `123456789`)

## Шаг 3: Создание файлов (2 мин)

Скопируйте содержимое файлов:
- `bot/bot.py` - основной код бота
- `bot/requirements.txt` - зависимости
- `bot/Dockerfile` - докер образ
- `docker-compose.yml` - конфигурация
- `.env.example` -> `.env` - настройки

## Шаг 4: Настройка .env (1 мин)

Создайте файл `.env` с **минимальной** конфигурацией:

```env
# Обязательные параметры
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
ALLOWED_USER_IDS=123456789
TRANSMISSION_USER=admin
TRANSMISSION_PASS=my_secure_password_123
```

**Замените:**
- `BOT_TOKEN` - на ваш токен от BotFather
- `ALLOWED_USER_IDS` - на ваш User ID
- `TRANSMISSION_PASS` - на свой пароль

Остальные параметры опциональны!

## Шаг 5: Запуск (30 сек)

```bash
docker-compose up -d
```

## Шаг 6: Проверка (30 сек)

```bash
# Проверьте логи
docker-compose logs -f telegram-bot

# Должно быть:
# 🚀 Запуск бота...
# 📡 Transmission: transmission:9091
```

Нажмите `Ctrl+C` чтобы выйти из логов.

## Шаг 7: Тестирование (1 мин)

1. Откройте вашего бота в Telegram
2. Отправьте: `/start`
3. Отправьте любую **magnet-ссылку**
4. Проверьте: `/list`

## ✅ Готово!

Теперь вы можете:
- Отправлять боту magnet-ссылки
- Смотреть список торрентов: `/list`
- Проверять статус: `/status`
- Получать уведомления при завершении загрузки

## 🔧 Дополнительные настройки

### Изменить интервал проверки
```env
CHECK_INTERVAL=30  # проверять каждые 30 секунд
```

### Кастомное приветствие
```env
WELCOME_MESSAGE=Привет! Отправь мне magnet-ссылку 🚀
```

### Изменить путь загрузок
```env
TRANSMISSION_DOWNLOADS_PATH=/mnt/storage/torrents
```

Смотрите `.env.example` для **всех 30 параметров**!

## 🐛 Что делать если не работает?

### Бот не отвечает
```bash
docker-compose logs telegram-bot
```
Проверьте `BOT_TOKEN` и `ALLOWED_USER_IDS`

### Не добавляются торренты
```bash
docker-compose logs transmission
```
Проверьте `TRANSMISSION_USER` и `TRANSMISSION_PASS`

### Нет прав на запись
```bash
id -u  # узнайте ваш PUID
id -g  # узнайте ваш PGID
```
Добавьте в `.env`:
```env
PUID=1000
PGID=1000
```

## 📖 Больше информации

Смотрите `README.md` для полной документации.
